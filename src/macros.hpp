/*
** EPITECH PROJECT, 2021
** easystream_main_plugin
** File description:
** macros
*/

#ifndef MACROS_HPP_
#define MACROS_HPP_

#define CLAMP(x, min, max) ((x) < min ? min : ((x) > max ? max : (x)))

#endif /* !MACROS_HPP_ */
